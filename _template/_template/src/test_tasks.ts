//---------------
// ==========   Classes intro
// Приклад. Створити товар. Для товару зберігаються назва товару,  ціна, кількість та вартість зберігання в день. Передбачити можливість знаходження загальної вартості наявної кількості одиниць, визначення вартості зберігання для заданої кількості днів, зменшення ціни на вказану кількість відсотків та збільшення ціни на вказану кількість відсотків

// ==========
// Приклад. Створити об’єкт товар.
// ----------- Властивості - характеристики ---------
// назва товару
// ціна
// кількість
// вартість зберігання в день
// ------------ Методи (функіональні можливості) -------------
// визначення загальної вартості наявної кількості одиниць
// визначити вартість зберігання для заданої кількості днів
// зменшення ціни на вказану кількість відсотків
// збільшення ціни на вказану кількість відсотків

// ==========
// Приклад. Описати об’єкт «Інвойс»
// ----- Властивості-характеристики (дані) ----
// номер інвойса
// перелік виконаних робіт
// сума грошей
// ----- Методи --------------------
// перетворення у рядок (вивести номер інвойса, кількість виконаних робіт, сума грошей
// перетворення у число (повертається сума грошей)

// Використовуючи такий опис створити масив інвойсів. Вивести інформацію про інвойси у формі нумерованого
// списку  та знайти загальну вартість використовуючи метод перетворення об’єкта у число
// ==========
// ==========
// ==========  private ============
// ==========
// ==========
// Приклад. Гра «Рулетка»
// ----- Властивості -------
// кількість полів рулетки (приватне)
// мінімальне значення балів (приватне)
// максимальне значення балів (приватне)
// список згенерованих значень (відкрите)

// ----- Методи ----------
// генерування полів рулетки (приватне)
// виведення списку згенерованих значень (відкрите)
// приведення до рядка (відкрите)
// знаходженя середнього значення для списку згенерованих балів (відкрите)
// крутити рулетку (отримання випадкового балу) (відкрите)

// ==========
// Приклад. Розробити клас «Передбачувач». Дозволяє кожні вказані кількість секунд отримувати передбачення
// Властивості :
// масив можливих передбачень,
// інтервал між передбаченнями
// Методи:AC
// вибір випадковоAго передбачення
// метод run, що ініціює запуск таймера і генерування передбачень

// ==========
// Задача. Створити клас TTime для роботи із часом у форматі “години:хвилини”. Час представляється структурою із двома полями. Реалізувати методи збільшення/зменшення  часу на певну кількість годин чи хвилин.

// ==========
//           Приклад. Створити об’єкт учень.
//        ----------- Властивості - характеристики ---------
//        ПІБ (прізвище, ім"я, по-батькові)
//        клас, у якому навчається
//        вік
//        середній бал

//        ------------ Методи (функіональні можливості) -------------

//        визначення того, ким він є (відмінник, хорошист, ...)
//        визначити кількості років до закінчення школи

// ==========
// Приклад. Створити клас «Клієнт»
// (ім’я – довільний доступ (відкрите поле),
// номер рахунку – тільки для читання,
// кількість грошей – контрольований доступ (і читання і запис))

// ==========
// Приклад. Створити клас «Клієнт»
// (ім’я – довільний доступ (відкрите поле),
// номер рахунку – тільки для читання,
// кількість грошей – контрольований доступ (і читання і запис))

// ==========
// Задача 2. Створити клас Product, що представляє товар на складі
// поля:
// 	Назва товару
// 	Кількість одиниць
// 	Ціна одиниці
// методи:
// 	зменшення кількості товару
// 	збільшення кількості товару
// 	визначення вартості вказаної кількості товару
// 	нарахування вказаної знижки (у відсотках)
// 	визначення загальної вартості товару

// ==========
// Створити клас TMoney для роботи з грошовими сумами. Сума повинна зберігатися у вигляді доларового еквіваленту. Реалізувати методи додавання/вилучення грошової маси, вказуючи необхідну суму у гривнях, та визначення курсу долара, при якому сума у гривнях збільшиться на 100. Курс долара зберігати в окремому полі.
// ==========
// ==========
// ==========
// ========== Параметричні властивості ====
// ==========
// ==========
// ==========
// ==========
// Задача.  Описати клієнта банку
// ----- Властивості ------
// ПІБ
//       * прізвище
//       * ім’я
// адреса
//       * код
//       * місто
//       * вулиця
//       * номер будинку
// номер рахунку
// кількість грошей

// Задача.  Дано список учнів школи.У школі
// проводиться декілька гуртків. Учень може
// брати участь у декількох гуртках. Реалізувати
// додавання/вірахування учнів з гуртків
// ===== Учень ======
// прізвище
// ім’я
// номер класу

// ===== Гурток ======
// ----- Властивості ------
// назва
// день провдення
// список учасників-учнів. Список об’єктів
//    {
//       id         -  учасника
//       data     - інформація про учасника
//    }
// ---- Методи -----
// додвання учня
// відрахування учня
// перевірка належності учня до гуртка

// ==========
// Задача. Команда. Дано список студентів (ім"я, кількість балів). По черзі випадковим чином вибирають дві команди.
//  Виграє той, у кого середній бал менше

// ==========

// Задача. Склад. База товарів, які зберігаються на складі: назва товару, одиниця виміру, кількість, фірма виробник
// (назва, реєстраційний номер). Організувати реєстрацію/відвантаження товарів, фільтрація за назвою товару,
// фільтрація за назвою фірми.

// ==========
// ==========
// ==========
// ========== this
// Задача. Розробити клас, який має метод для генерування випадкового числа у деякому діапазоні. Прив’язати цей метод до кнопки.
// ==========
// Задача. Розробити клас, який приймає порогове значення і має метод для аналізу вводу у input. Якщо введене значення менше за порогове, то фон має бути червоним, інакше – зеленим.
// ==========

// ========== static =========
// Лічильник створених екземплярів класу. Використовується для відстеження кількості створених об’єктів певного типу.
// ==========
// Фабричний метод створення екземплярів. Клас може мати статичний метод, який створює об’єкт із попередньо налаштованими параметрами.
// ==========
// Менеджер конфігурації. Використовується для зберігання глобальних налаштувань додатку (властивість settings, метод updateSettings, окремі методи для оновлення окремих полів).
// ==========
// Валідація величин без створення екземпляра. Статичний метод дозволяє перевіряти дані перед створенням об’єкта.
// ==========
// Singleton через статичне поле. Використовується для створення єдиного доступного об’єкта (наприклад, масиву для зберження елементів +  відповідні методи додавнаня, вилучення, фільтрації).
// Автоматичне формування ID. Статичне поле генерує унікальні ідентифікатори (спочатку 0, потім інкремент).
// ==========
// Константи-аналог enum через статичні поля. Можна створювати набори констант, не використовуючи enum. Наприклад, HttpStatus .
// ==========
// Логування через статичний метод. Методи для info, warn, error.

// ==========
// ==========
// ==========
// ==========
// ==========
// 1. Система управління замовленнями в Інтернет-магазині
// Клас: Order (Замовлення). Має поля: orderId, customer, items, totalAmount. Містить методи для обчислення загальної суми та зміни статусу.

// Enum: OrderStatus (СтатусЗамовлення). Визначає можливі стани замовлення: PENDING (Очікує), PROCESSING (Обробляється), SHIPPED (Відправлено), DELIVERED (Доставлено), CANCELLED (Скасовано).
// ==========
// 2. Моделювання Світлофора 🚦
// Клас: TrafficLight (Світлофор). Містить поле currentState і метод changeState(), який переключає світло на наступний дозволений колір.

// Enum: LightColor (КолірСвітла). Визначає фіксовані кольори: RED (Червоний), YELLOW (Жовтий), GREEN (Зелений).
// ==========
// 4. Обробка Фінансових Транзакцій 💳
// Клас: Transaction (Транзакція). Містить поля: id, amount, date, type, description. Містить метод для валідації суми.

// Enum: TransactionType (ТипТранзакції). Визначає напрямок або вид транзакції: DEPOSIT (Поповнення), WITHDRAWAL (Зняття), TRANSFER (Переказ).
// ==========
// 5. Система Управління Документами 📄
// Клас: Document (Документ). Має поля: title, content, author, status. Містить методи для архівування або публікації.

// Enum: DocumentStatus (СтатусДокумента). Визначає етапи життєвого циклу: DRAFT (Чернетка), REVIEW (НаПеревірці), PUBLISHED (Опубліковано), ARCHIVED (ВАрхіві).
// ==========
// 6. Моделювання Персонажів у RPG (Role-Playing Game) 🧙‍♂️
// Клас: Character (Персонаж). Має поля: name, health, mana, charClass. Містить методи attack() та takeDamage().

// Enum: CharacterClass (КласПерсонажа). Визначає професії/класи: WARRIOR (Воїн), MAGE (Маг), ARCHER (Лучник), ROGUE (Розбійник).

// ==========
// 7. Конфігурація Журналування (Логування) 🛠️
// Клас: Logger (Логер). Статичний або Singleton-клас, який приймає повідомлення та його рівень, і виводить його у консоль або файл.

// Enum: LogLevel (РівеньЛогування). Визначає важливість повідомлення: DEBUG (Налагодження), INFO (Інформація), WARN (Попередження), ERROR (Помилка).
// ==========
// 8. Робота з Налаштуваннями Користувача ⚙️
// Клас: UserSettings (НалаштуванняКористувача). Містить поля для зберігання налаштувань (наприклад, theme, language, notificationsEnabled). Методи для завантаження/збереження.

// Enum: AppTheme (ТемаДодатку). Визначає візуальні теми: LIGHT (Світла), DARK (Темна), SYSTEM (Системна).
// ==========
// 9. Управління Складом та Активами 📦
// Клас: Asset (Актив). Має поля: id, name, location, assetType. Методи для переміщення та інвентаризації.

// Enum: AssetType (ТипАктиву). Категоризація об'єктів: ELECTRONICS (Електроніка), FURNITURE (Меблі), VEHICLE (Транспорт).
// ==========
// 10. Обробка Запитів до API (HTTP-клієнт) 🌐
// Клас: ApiClient (КлієнтAPI). Містить методи sendRequest(), які використовують певний HTTP-метод та URL.

// Enum: HttpMethod (МетодHTTP). Визначає стандартні методи: GET, POST, PUT, DELETE, PATCH.
// ==========
// ==========  class+interfaces
// ==========
// 1. Сервіс налаштувань користувача ⚙️
// Інтерфейс: IUserSettings – визначає структуру налаштувань, які потрібно зберігати (theme: 'dark' | 'light', language: string, notifications: boolean).

// Клас: SettingsService – містить метод loadSettings(): IUserSettings та saveSettings(settings: IUserSettings). Клас не є самими налаштуваннями, але оперує об'єктами цього типу.
// ==========
// 2. Клієнт для роботи з API 🌐
// Інтерфейс: IAPIResponse – визначає очікуваний формат відповіді від сервера (наприклад, status: number, data: any, errorMessage?: string).

// Клас: HttpClient – має метод fetchData(url: string): Promise<IAPIResponse>. Клас виконує HTTP-запит і повертає об'єкт, який відповідає контракту IAPIResponse.
// ==========
// 3. Обробник черги повідомлень 📧
// Інтерфейс: IMessage – визначає структуру повідомлення, яке має бути оброблено (topic: string, payload: object, timestamp: Date).

// Клас: MessageQueueProcessor – має метод process(message: IMessage). Клас не створює повідомлення, але очікує на вхідні дані, що відповідають IMessage.
// ==========
// 5. Система кешування даних (Cache Manager) 💾
// Інтерфейс: ICacheEntry – визначає структуру елемента, що зберігається в кеші (value: any, expiryDate: Date).

// Клас: CacheManager – має методи get(key: string): ICacheEntry | undefined та set(key: string, value: any, ttl: number).
// ==========
// 6. Конфігуратор бази даних 🗃️
// Інтерфейс: IDatabaseConfig – визначає необхідні параметри підключення (host: string, port: number, username: string, password?: string).

// Клас: DBConnection – містить приватне поле config: IDatabaseConfig і метод connect(). Клас використовує конфігурацію, тип якої задано інтерфейсом.
// ==========
// 9. Фабрика звітів (Report Factory) 📊
// Інтерфейс: IReportData – визначає уніфікований формат даних, які будуть використані для генерації звіту (title: string, rows: any[][], metadata: object).

// Клас: ReportGenerator – має метод generateHTML(data: IReportData).
// ==========
// ==========  class+union+intersections
// ==========
// 1. Менеджер стану користувача 👤
// Клас: UserManager
// Складний тип: Визначення типу UserStatus.
// UserStatus = { state: 'online', lastActive: Date } | { state: 'offline', lastActive: Date } | { state: 'away' }; (Об'єднання для різних станів)

// Метод: setUserStatus(id: number, status: UserStatus)
// ==========
// 2. Обробник різних типів вхідних даних 📨
// Клас: DataProcessor

// Складний тип: Визначення типу ProcessableInput.

// ProcessableInput = string | number | string[] (Об'єднання для роботи з рядком, числом або масивом рядків)

// Метод: processData(input: ProcessableInput) – метод повинен використовувати перевірку типів (typeof, instanceof) для обробки кожного варіанту.
// ==========
// 3. Налаштування підключення з обов'язковим логуванням 🗃️
// Клас: DatabaseConnector

// Складний тип: Визначення типу ConnectionConfig.

// Базовий тип: BaseConfig = { host: string, port: number }

// Додатковий функціонал: LoggingConfig = { enableLogging: true, logger: (msg: string) => void }

// Перетин: ConnectionConfig = BaseConfig & LoggingConfig (Обов'язкова наявність як базових полів, так і логування)

// Метод: connect(config: ConnectionConfig)
// ==========
// 5. Система управління сповіщеннями 🔔
// Клас: NotificationService

// Складний тип: Визначення типу NotificationPayload.

// EmailPayload = { recipient: string, subject: string }

// SMSPayload = { phone: string, text: string }

// Об'єднання та Перетин: NotificationPayload = ({ type: 'email' } & EmailPayload) | ({ type: 'sms' } & SMSPayload) (Дискримінаційне об'єднання для безпечної роботи з різними типами сповіщень)

// Метод: sendNotification(payload: NotificationPayload)
// ==========
// 6. Моделювання співробітників з різними ролями 👨‍💼
// Клас: CompanyDirectory

// Складний тип: Визначення типу Employee.

// BaseEmployee = { id: number, name: string }

// ManagerFields = { department: string, subordinates: number }

// EngineerFields = { stack: string[], level: 'junior' | 'senior' } (Об'єднання для рівня)

// Об'єднання та Перетин: Employee = (BaseEmployee & ManagerFields) | (BaseEmployee & EngineerFields) (Співробітник має або менеджерські, або інженерні поля)

// Метод: getEmployeeDetails(employee: Employee)
// ==========
// 8. Менеджер файлових операцій 📁
// Клас: FileManager

// Складний тип: Визначення типу FileAccessOptions.

// BaseOptions = { encoding: 'utf8' | 'binary' } (Об'єднання для кодування)

// WriteOptions = { overwrite: boolean }

// Перетин: WriteConfig = BaseOptions & WriteOptions (Конфігурація для запису вимагає і кодування, і параметра перезапису)

// Метод: writeFile(path: string, content: string, options: WriteConfig)
// ==========
// 10. Модель валідатора об'єктів 🧪
// Клас: Validator
// Складний тип: Визначення типу ValidationResult.
// SuccessResult = { success: true }
// FailureResult = { success: false, errors: string[] | null } (Об'єднання для поля errors)
// Об'єднання: ValidationResult = SuccessResult | FailureResult
// Метод: validate(object: object): ValidationResult
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
// ==========
