// 🔌 1. Підключення до різних платіжних систем
// У тебе є класи PayPalService, StripeService, CryptoGateway, які мають різні інтерфейси.
//  Потрібно створити адаптери (PayPalAdapter, StripeAdapter, CryptoAdapter),
// щоб уся система могла працювати через єдиний інтерфейс PaymentProvider.
// Мета - показати, як адаптер уніфікує зовнішні API.

export default interface IPaymentProvider {
  pay(amount: number): void;
}
